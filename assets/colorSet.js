import { Colors } from './colors';

export const ColorSet = {
  // scanned item
  itemColor: Colors.black,
  barcode: Colors.black,

  // grid
  gridTextColor: Colors.white,
  oddMonth: Colors.darkerRed,
  evenMonth: Colors.darkRed,
  lastItemBackground: Colors.grey,
  deleteItemBackground: Colors.grey,

  // reset button
  resetButtonBackground: Colors.darkerRed,
  resetButtonText: Colors.white,

  // selected date
  dateValueColor: Colors.black,

  // native date picker
  dateTimePickerColor: Colors.darkRed,
};
